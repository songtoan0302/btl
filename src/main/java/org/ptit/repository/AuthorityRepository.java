package org.ptit.repository;

import org.ptit.domain.Authority593;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Spring Data JPA repository for the {@link Authority593} entity.
 */
public interface AuthorityRepository extends JpaRepository<Authority593, String> {}
